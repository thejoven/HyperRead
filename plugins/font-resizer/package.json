{
  "name": "font-resizer",
  "version": "1.0.0",
  "description": "字号调节插件",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --format=esm --outfile=main.js"
  },
  "devDependencies": { "esbuild": "^0.19.0" }
}
